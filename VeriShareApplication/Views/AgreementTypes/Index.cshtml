@model IEnumerable<VeriShareApplication.Models.AgreementType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AgreementName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AgreementName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AgreementTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AgreementTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AgreementTypeId })
        </td>
    </tr>
}

</table>
